Run the **discovery-synthesis** skill (idea-discovery-playbook).

Compile one researcher pack from all artifacts under `discovery/<slug>/`. Use text after `/discovery-synthesis` for topic slug.

Save to `discovery/<slug>/YYYY-MM-DD-<slug>-synthesis.md`.
