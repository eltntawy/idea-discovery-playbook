Run the **discovery-synthesis** skill (ideation-discovery-skills).

Compile one researcher pack from all artifacts under `discovery/<slug>/`. Use text after the command for topic slug.

Save to `discovery/<slug>/YYYY-MM-DD-<slug>-synthesis.md`.
