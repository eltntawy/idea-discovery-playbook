Run the **assumption-map** skill (ideation-discovery-skills).

List top assumptions, evidence tiers, and cheapest falsifying tests. Use text after the command for the idea and topic slug (propose slug if missing).

Save to `discovery/<slug>/research/YYYY-MM-DD-<slug>-assumptions.md`.
