Run **validation-experiments** (idea-discovery-playbook).

$ARGUMENTS

Priced fake door or pre-sale; success/fail thresholds; ILI. Save `discovery/<slug>/experiments/YYYY-MM-DD-<slug>-experiment.md`.
