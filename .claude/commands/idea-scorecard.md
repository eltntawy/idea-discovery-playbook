Run **idea-scorecard** (ideation-discovery-skills). Read `discovery/<slug>/` artifacts first.

$ARGUMENTS

Weighted /75 scorecard and go / conditional / pivot / kill. Save `discovery/<slug>/scorecards/YYYY-MM-DD-<slug>-scorecard.md`.
